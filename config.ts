import { IConfig } from "./src/types";

export const config: IConfig = {
  discord_token: "xxxxx", // Discord bot token https://discord.com/developers/docs/intro
  spotify_credentials: "xxxxx", // Spotify dev credentials - convert "{client_id}:{client_secret}" to base64
  email: "xxxx@xx", // tt.live user email
  password: "xxxxxxxx", // tt.live user password
};
